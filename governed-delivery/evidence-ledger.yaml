# receipt = where the actual tool output lives. `not_retained` means the claim
# rests on a session summary and must be re-run before it can release a gate.
# Entries are appended by flow.sh (state/NN-*.log) and by hand for out-of-band
# evidence. Never edit an existing entry; append a superseding one.
entries:
  - claim: "facet_header_covers_cgen_scanner_facts_ratchet passes with allStrArr 0/0, strDual scanner-only 0 / facet-only 153, xst 0/0 — all three now asserted"
    source: "cargo test --release -p xb-compiler --test facet_scanner_gap -- --nocapture"
    attester: "cargo test runner"
    revision: "working tree after 06fa96c (facet_scanner_gap.rs strengthened)"
    exit_status: 0
    receipt: "session tool output 2026-09-05 (234 programs; test result: ok. 1 passed)"
    freshness_kind: recheck_date
    date_checked: "2026-09-05"
  - claim: "checks/validate-all.sh fails closed when cargo exits non-zero without results, when no binaries ran, and when link-core-libs.sh fails; passes with exit 0 on a stubbed green run"
    source: "fault-injection rig over the unmodified script prefix (5 cases)"
    attester: "sh (deterministic)"
    revision: "working tree after 06fa96c"
    exit_status: "1,1,1,1 on injected failures; 0 on stub happy path"
    receipt: "session tool output 2026-09-05"
    freshness_kind: recheck_date
    date_checked: "2026-09-05"
  - claim: "docs_headline_claims_are_recorded_at_named_surfaces passes"
    source: "cargo test --release -p xb-runtime --test cgen_cemitter_sync -- docs_headline_claims"
    attester: "cargo test runner"
    revision: "06fa96c"
    exit_status: 0
    receipt: "session tool output 2026-09-05"
    freshness_kind: recheck_date
    date_checked: "2026-09-05"
  - claim: "cgen_cemitter_sync 85/85; cgen_demo_regression 27/27; validate-all.sh 339 tests/40 binaries + 15/15 libs; verify-bootstrap.sh ok"
    source: "prior session runs"
    attester: "not_retained — session self-report"
    revision: "a92168e"
    exit_status: unknown
    receipt: not_retained
    freshness_kind: unknown
    date_checked: "2026-09-05 (reported); rerun required before any gate release"
  - claim: "LLVM feature gate green (144 tests, one documented ignore)"
    source: "CI llvm-build job"
    attester: "GitHub Actions"
    revision: unknown
    exit_status: unknown
    receipt: "no run URL recorded"
    freshness_kind: unknown
    date_checked: unknown
  - claim: "ATTACH is move semantics (src emptied, dst must be empty, jagged trailing-comma rows)"
    source: "xbasic/helpsrc/help_text/lang.txt:57-62, :2244-2260"
    attester: "upstream source text"
    revision: "xbasic/ tracked tree"
    receipt: "file content"
    freshness_kind: immutable_pin
    date_checked: "2026-09-05"
  - claim: "Move semantics correct on ary.x-class programs through the compiled binary"
    source: "maintainer spot-check"
    attester: "teee (user-reported)"
    revision: "a92168e"
    receipt: not_retained
    freshness_kind: recheck_date
    date_checked: "2026-09-05"
  - claim: "docs/17 rows CORE-LIBS-CC, ARY-STATUS-RECONCILIATION, RR-08a, RR-08b restored to full length from 5caa152/a92168e/799b967/08c6b1e; zero unterminated table rows remain"
    source: "git show + splice; scan for rows not ending in '|'"
    attester: "script (deterministic)"
    revision: "working tree after 06fa96c"
    exit_status: 0
    receipt: "session tool output 2026-09-05 (tableRowsNotEndingInPipe: [])"
    freshness_kind: recheck_date
    date_checked: "2026-09-05"
  - claim: "Modified checks/validate-all.sh passes for real: cargo gate + 15/15 compile+link + Tier-1 symbol guard"
    source: "./checks/validate-all.sh"
    attester: "validate-all.sh (deterministic; fixed exit-status propagation)"
    revision: "working tree after 06fa96c (this change set)"
    exit_status: 0
    receipt: "session tool output 2026-09-05, 488 s; smoke 3/6 Version$ mismatches tolerated by design (link-core-libs.sh L36-42)"
    freshness_kind: recheck_date
    date_checked: "2026-09-05"
  - claim: "checks/verify-bootstrap.sh ok: 339 passed / 40 binaries / 0 failing; strengthened facet ratchet ok; Rust IR == compA == compB; cgen1 C == cgen2 C"
    source: "./checks/verify-bootstrap.sh"
    attester: "verify-bootstrap.sh (deterministic)"
    revision: "working tree after 06fa96c (this change set)"
    exit_status: 0
    receipt: "/tmp/xb-verify-bootstrap.log (session), 540 s"
    freshness_kind: recheck_date
    date_checked: "2026-09-05"
  - claim: "BYVAL-ARRAY-COPY slice passes for real: checks/validate-all.sh ALL PASS (344 tests across 41 binaries, incl. 4 new byvalue/ubound locks)"
    source: "./checks/validate-all.sh"
    attester: "validate-all.sh (deterministic)"
    revision: "working tree after 6ab940c (this change set)"
    exit_status: 0
    receipt: "/tmp/xb-validate-byval.log (session); core libs 15/15; Tier-1 guard passed (1979 symbols, pre-existing variance)"
    freshness_kind: recheck_date
    date_checked: "2026-09-05"
  - claim: "checks/verify-bootstrap.sh ok with byval slice: fmt/clippy/debug-suite green; cgen1 C == cgen2 C fixed point exact"
    source: "./checks/verify-bootstrap.sh"
    attester: "verify-bootstrap.sh (deterministic)"
    revision: "working tree after 6ab940c (this change set)"
    exit_status: 0
    receipt: "/tmp/xb-verify-byval.log (session)"
    freshness_kind: recheck_date
    date_checked: "2026-09-05"
  - claim: "AC4 import filter removed + import-only composite stub fix passes for real: checks/validate-all.sh ALL PASS (345 tests across 41 binaries, incl. cli_import_composite_signatures_emit_clean_stub)"
    source: "./checks/validate-all.sh"
    attester: "validate-all.sh (deterministic)"
    revision: "working tree after af4487a (this change set)"
    exit_status: 0
    receipt: "/tmp/xb-validate-ac4.log (session); core libs 15/15; Tier-1 guard passed"
    freshness_kind: recheck_date
    date_checked: "2026-09-05"
  - claim: "checks/verify-bootstrap.sh ok with AC4 slice: fmt/clippy/debug-suite green; cgen1 C == cgen2 C fixed point exact"
    source: "./checks/verify-bootstrap.sh"
    attester: "verify-bootstrap.sh (deterministic)"
    revision: "working tree after af4487a (this change set)"
    exit_status: 0
    receipt: "/tmp/xb-verify-ac4.log (session)"
    freshness_kind: recheck_date
    date_checked: "2026-09-05"
  - claim: "Dual-divert + heap-bare slice passes for real: checks/validate-all.sh ALL PASS (348 tests across 41 binaries, incl. 3 new byref-forward locks)"
    source: "./checks/validate-all.sh"
    attester: "validate-all.sh (deterministic)"
    revision: "working tree after 721088d (this change set)"
    exit_status: 0
    receipt: "/tmp/xb-validate-divert5.log (session); core libs 15/15; Tier-1 guard passed"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "checks/verify-bootstrap.sh ok with divert slice: fmt/clippy/debug-suite green; cgen1 C == cgen2 C fixed point exact"
    source: "./checks/verify-bootstrap.sh"
    attester: "verify-bootstrap.sh (deterministic)"
    revision: "working tree after 721088d (this change set)"
    exit_status: 0
    receipt: "/tmp/xb-verify-divert.log (session)"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "Storage-scope tightening + alias lock pass for real: checks/validate-all.sh ALL PASS (349 tests across 41 binaries)"
    source: "./checks/validate-all.sh"
    attester: "validate-all.sh (deterministic)"
    revision: "working tree after c7161b2 (this change set)"
    exit_status: 0
    receipt: "/tmp/xb-validate-tight.log (session); core libs 15/15; Tier-1 guard passed"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "checks/verify-bootstrap.sh ok with tightening: fmt/clippy/debug-suite green; cgen1 C == cgen2 C fixed point exact"
    source: "./checks/verify-bootstrap.sh"
    attester: "verify-bootstrap.sh (deterministic)"
    revision: "working tree after c7161b2 (this change set)"
    exit_status: 0
    receipt: "/tmp/xb-verify-tight.log (session)"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "at_scalar lock included passes for real: checks/validate-all.sh ALL PASS (350 tests across 41 binaries)"
    source: "./checks/validate-all.sh"
    attester: "validate-all.sh (deterministic)"
    revision: "working tree after a05a099 (this change set)"
    exit_status: 0
    receipt: "/tmp/xb-validate-350.log (session); core libs 15/15; Tier-1 guard passed"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "checks/verify-bootstrap.sh ok with at_scalar lock: fmt/clippy/debug-suite green; cgen1 C == cgen2 C fixed point exact"
    source: "./checks/verify-bootstrap.sh"
    attester: "verify-bootstrap.sh (deterministic)"
    revision: "working tree after a05a099 (this change set)"
    exit_status: 0
    receipt: "/tmp/xb-verify-350.log (session)"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "SUPERSEDES revision attribution of the two AC4 entries above: the composite-signature import-filter removal + import-only composite stub fix landed in 721088d (M1-AC4), whose parent is af4487a — not 'after af4487a'. Filter absence re-confirmed at HEAD (no sigfilter/sign_filter/filter-inject matches in crates/xb-compiler/src/c_emit.rs or selfhost/cgen.x); lock test cli_import_composite_signatures_emit_clean_stub passes at HEAD"
    source: "git show --stat 721088d + git rev-parse 721088d^ (= af4487a); grep -rni sigfilter/sign_filter/filter-inject (zero matches); cargo test --release -p xb-cli --test cli -- cli_import_composite_signatures_emit_clean_stub --test-threads=1"
    attester: "git + grep (deterministic) + cargo test runner"
    revision: "1d2ff85 (HEAD; fix commit 721088d)"
    exit_status: 0
    receipt: "session tool output 2026-09-06 (test result: ok. 1 passed); grep FILTER_SEARCH_DONE with zero matches"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "CORRECTS the previous SUPERSEDES entry (which wrongly charged an off-by-one): the ledger's 'working tree after X' convention is parent-HEAD-plus-uncommitted-slice, gates run pre-commit — verified: af4487a^=6ab940c, c7161b2^=721088d, 1dd83ae^=c7161b2, 721088d^=af4487a, each matching its entries' cited revision. The convention is consistent, not erroneous. Residual: at_scalar entries cite 'after a05a099' but e9f5daa^=c2f5d79 (sequence a05a099->c2f5d79->e9f5daa); whether gates ran at a05a099+uncommitted or c2f5d79+uncommitted is unattestable from retained receipts (/tmp logs gone) — re-run before any gate release per header rule"
    source: "git rev-parse <slice>^ for af4487a, c7161b2, 1dd83ae, 721088d, e9f5daa; git log --reverse a05a099..e9f5daa"
    attester: "git (deterministic)"
    revision: "1d2ff85 (HEAD; ledger-only change)"
    exit_status: 0
    receipt: "session tool output 2026-09-06 (parent hashes listed)"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "AC2 ratchet re-verified at HEAD: allStrArr 0/0, strDual scanner-only 0 / facet-only 153, xst 0/0 — identical to 2026-09-05 baseline. AC2 remains open (153 facet-only names; scanner deletion still blocked on compiler.x facet emission + maintainer semantic decision per intent-record unknowns)"
    source: "cargo test --release -p xb-compiler --test facet_scanner_gap -- --nocapture"
    attester: "cargo test runner"
    revision: "3355584 (HEAD)"
    exit_status: 0
    receipt: "session tool output 2026-09-06 (test result: ok. 1 passed)"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "CGEN-OOM root cause: native cgen.x on xgr.x peaked at 7.6 GB RSS / 585 G instructions / 37 s; xui 7.46 GB / 55 s; xit 7.93 GB / 23 s (the three libs killed with exit None under cgen_x_compiles_core_libs_floor_9_cc_clean). RR-03 filter_dyn_scope$ called facet_has_entry$ per scanner-dyn name per function, each re-tokenizing the whole facet table; its result was compared `= 0` (string vs integer -> LEN()=0, always false in interp, CEmitter and cgen - three-engine probe), so the branch was dead weight. Emitted C never frees string temporaries (10 free( in cgen.c), so RSS = cumulative temporaries"
    source: "/usr/bin/time -l on $TMPDIR/xb_sync_cgen_shared/cgen < xb --emit-ir-facets <lib>; sample(1) call graph; xb_alloc backtrace sampling (1/1024); /tmp/probe_strint.x on three engines"
    attester: "session measurement 2026-09-06"
    revision: "83b3aec (pre-fix cgen.x)"
    exit_status: 0
    receipt: "session tool output 2026-09-06 (maximum resident set size 7602110464 for xgr; 'ne0 ne0' on all three engines)"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "CGEN-OOM fix (selfhost/cgen.x only): dead facet_has_entry$ branch + function deleted, facet_scope_sets$ single pass per function replaces four facets_in_scope$ scans (scope tested before allocating), facet lines trimmed once at table build, replace$ index-based, write-only ##facetDynAll$ removed. Emitted C byte-identical on xgr/xit/xui (cmp against pre-fix cgen output). After: xgr 1.96 GB / 4.4 s, xit 1.77 GB / 3.7 s, xui 3.63 GB / 7.7 s"
    source: "xb --emit-c selfhost/cgen.x | cc; cmp /tmp/<lib>_old.c /tmp/<lib>_final.c; /usr/bin/time -l"
    attester: "session measurement 2026-09-06"
    revision: "working tree after 83b3aec (this change set)"
    exit_status: 0
    receipt: "session tool output 2026-09-06 (xgr/xit/xui IDENTICAL; RSS figures above)"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "Gates green on the fixed tree: cgen_x_compiles_core_libs_floor_9_cc_clean 15/15 (27.8 s, was killed); verify-bootstrap.sh ok (41 suites, cgen1==cgen2, IR identity); validate-all.sh ALL PASS 371 tests / 41 binaries + core libs 15/15 link; cargo fmt --check clean; clippy --workspace --all-targets 0 warnings. New lock cgen_x_peak_rss_on_core_libs_stays_bounded (crates/xb-runtime/tests/cgen_selfhost.rs, non-constitutional): xgr.x < 4 GiB (fixed 1.8 GiB; pre-fix HEAD~1 cgen.x fails at 5.93 GiB, uncontended) and xui.x < 5 GiB (fixed 3.6 GiB; pre-fix 6.98 GiB fails); ceilings keep two-sided margin because host memory pressure lowered a contended pre-fix xui reading below 6 GiB and an uncontended xgr reading to 5.9 GiB vs 7.1 GiB under time -l"
    source: "cargo test --release -p xb-runtime --test cgen_cemitter_sync -- --test-threads=1 cgen_x_compiles_core_libs_floor_9_cc_clean; bash checks/verify-bootstrap.sh; bash checks/validate-all.sh; cargo test --release -p xb-runtime --test cgen_selfhost -- --test-threads=1"
    attester: "cargo test runner / gate scripts"
    revision: "working tree after 83b3aec (this change set)"
    exit_status: 0
    receipt: "/tmp/vb.log (verify-bootstrap: ok), /tmp/va.log (=== ALL PASS (371 tests across 41 binaries) ===), session tool output 2026-09-06"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "Roadmap review 2026-09-06 applied to non-constitutional surfaces: docs/17 open-rows table syntax repaired (never rendered), stale duplicate status cells removed, spliced panel sentence unspliced, CGEN-FACET-RETIREMENT row refreshed, 2026-08-31 snapshot narrative (140 lines) and sections 1-4 (560 lines) archived verbatim to docs/22 under P10 / CAL-20260901-09 with one-line pointers kept (docs/17 1719 -> 1030 lines; all guard anchors retained), L15 paths updated to xbasic/lib/, §0 preamble made truthful; docs/20 M1 body/WP1 aligned with §6.1, exit-gate bullets annotated with verification state and the pending definition decision, packet-vs-exit-gate discrepancy recorded, §6.2 strDual wording settled, M2 done items marked; docs/19 §9 numbering unified (P6 flip / P7 out of scope) and memory risk added; docs/README authority map lists docs/22; intent-record unknowns updated (strDual resolved_pending_signature; M1 definition and packet reconciliation added) - status stays unsigned; failure-log count refreshed"
    source: "git diff d90a74b..HEAD (docs/, governed-delivery/intent-record.yaml, governed-delivery/failure-log.yaml)"
    attester: "session edits 2026-09-06; docs_adopted_wording_guard + docs_headline_claims re-run"
    revision: "working tree after d90a74b (this change set)"
    exit_status: 0
    receipt: "session tool output 2026-09-06 (both doc guards ok)"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "PROPOSED constitutional updates (NOT applied - GDR-1 / envelope pause action; maintainer applies out-of-band): (1) task-packet.yaml line 4 'docs/20 §3 M1 lines 145-154' -> 'lines 147-172'; line 32 '(85 sync oracles)' -> '(115 sync oracles)'; AC1 status comment '# not yet re-verified for this delivery' -> '# re-verified 2026-09-06 at 9f6d816: validate-all 371/41, verify-bootstrap ok (acceptance pending)'; AC3 comment add '# 11 named probes re-verified 2026-09-06'; AC4 comment '# filter removal not done; fixed point green at baseline' -> '# filter removed 721088d; fixed point green at 9f6d816 (acceptance pending)'; AC5: either delete (docs/20 exit gate has 4 bullets; docs/17 says residuals not blocking M1 exit) or add its text to docs/20 §3 M1 - maintainer decision. (2) oracle-manifest.yaml line 24 'cgen_cemitter_sync (85 tests)' -> '(115 tests)'. (3) acceptance-record.yaml lines 6-7: 'execution queue, lines 229-258' -> 'lines 98-127'; 'M1 exit gate, lines 145-154' -> 'lines 147-172'. (4) README.md 'Full workspace:** 308 passed / 0 failed' is stale (371/41) but pinned verbatim by docs_headline_claims_are_recorded_at_named_surfaces - update README and the test needle together"
    source: "governed-delivery/oracle-manifest.yaml constitutional_paths; envelope.yaml pause_actions"
    attester: "session review 2026-09-06"
    revision: "working tree after d90a74b"
    exit_status: 0
    receipt: "this entry is the proposal; no constitutional file modified (git diff --stat confirms)"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "P1 compiler.x facet emission landed (docs/19 §9.5): dump-only token pre-pass in selfhost/compiler.x behind ##FACETS## first-line hook (normal emission byte-identical vs HEAD through the interpreter); emits facet name:type scope=S storage=T rank=R dual=0[ shared] for array DIMs (storage syntactic fixed/shared from # prefix; rank counts top-level comma groups; DECLARE/EXTERNAL forward decls do not set scope; no param/member/descriptor facts). New non-constitutional lock crates/xb-runtime/tests/native_facet_gap.rs: native vs Rust allStrArr-field sets equal over selfhost tools + 81 positive-corpus programs (non-vacuous: compiler.x 12, cgen.x 3, ubound_test.x 1 names; 0.9 s). Mutation verified: fails on pre-P1 compiler.x (native empty vs 12 Rust names). Normal-path gates: native_pipeline compA byte-identical, cgen_selfhost 3/3, facet_scanner_gap green; fmt clean, clippy 0. No cgen.x touch, no scanner deletion, no constitutional edit. Uncommitted per execute prompt"
    source: "selfhost/compiler.x P1 blocks; cargo test --release -p xb-runtime --test native_facet_gap,native_pipeline,cgen_selfhost -- --test-threads=1; cargo test --release -p xb-compiler --test facet_scanner_gap"
    attester: "cargo test runner"
    revision: "working tree after 3cfe265 (this change set, uncommitted)"
    exit_status: 0
    receipt: "session tool output 2026-09-06 (native_facet_gap ok 0.9 s; native_pipeline ok; cgen_selfhost 3/3; facet gap ok)"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "P1 complete: native_facet_gap allstrarr gate green over the full 234-program ratchet universe (41 nonzero, 158 names, 0 differing, 1 byref carve-out); dual split to an ignored P2 tracker (39 differing at split; dual=0 placeholder, P2 owns the use-based port). Rounds after the initial landing: per-scope order-sensitive shared sets (Rust symbol-global premise falsified against semantics_stmts.rs per-function fresh sets); SHARED-statement per-element brackets; comma-separated multi-name DIM/STATIC lists; array-param facets with DIM suppression; REDIM-as-Dim{shared:false}; STATIC (never forces shared); STRING-typename statements (type forced, TYPENAME # shares like DIM #); stripped scopes; EXIT FUNCTION/prototype scope guards (a dropped-guard incident silently poisoned scopes to '' and was caught by re-bisect, not by the order-blind first smoke); $$-suffix forces giant; member (dotted) + byref carve-outs per §9.4. Robustness fixes required by the widened corpus, all normal-path-neutral (native_pipeline byte-identical): DECLARE/EXTERNAL/INTERNAL prototype skip (was infinite hang on typed params), expr drain-loop unclosed-frame guard, stmtState=13 missing reset (was 5.9M-line/1.5GB flood), heap tables for srcLines/tt/tv/const/arrNames/fargs (xcol/xgr/xit/xui SIGSEGV). Known out-of-scope residuals (M5 language coverage, never green): normal-path library compilation (valStack[256] depth overflow on aarray_ISNODE expressions), compA normal-path xcol OOM-kill after 75K lines; emitter-level temporary-release memory work. Uncommitted per execute prompt"
    source: "cargo test --release -p xb-runtime --test native_facet_gap,native_pipeline,cgen_selfhost,bootstrap -- --test-threads=1; cargo test --release -p xb-compiler --test facet_scanner_gap; fmt --check; clippy --workspace --all-targets (0 warnings)"
    attester: "cargo test runner"
    revision: "working tree after 3cfe265 (this change set, uncommitted)"
    exit_status: 0
    receipt: "session tool output 2026-09-06 (allstrarr gate 4.3 s green; dual tracker ignored/39 differing; 8-case declarator matrix + DECLARE/IFZ/HEXX misfire probes green via interpreter; normal-mode IR smoke green)"
    freshness_kind: recheck_date
    date_checked: "2026-09-06"
  - claim: "BOOTSTRAP FIXED POINT was RED at HEAD from 8a7eb1e through 639989a (7 commits, the whole P2 series) and is now restored at 67110bb. Bisect (xb --emit-ir compiler.x vs compA self-run, per commit): 17a456c OK; 8a7eb1e BROKEN 780 diff lines; 2aefb32 774; f79d067 962; 022d456 2393; 677fb3f 962; 639989a 962. Root cause: P2 introduced the first GOSUB/label/GOTO/bare-RETURN constructs into selfhost/compiler.x, and compiler.x's own emission arms for GOSUB (was: two bare tpos increments), GOTO (same) and label-definition (ident+':' skip) CONSUMED TOKENS BUT EMITTED NOTHING, so native dropped all 8 gosub / 4 gosub-target / 1 goto / 4 label items Rust emits. Three further divergences surfaced once line counts aligned: (a) a stray unmatched `END IF` in the DIM declarator arm that Rust silently treats as a no-op while native re-nested everything after it (indent 22 vs 8 spaces, identical content); (b) `uPos$` used undeclared alongside `DIM uPos` (integer) — Rust keeps the `$` in the emitted symbol to disambiguate, native strips it, so 3 sites emitted `uPos:string`; renamed to `uArgPos$`; (c) bare `RETURN` lowers to GosubReturn (semantics.rs), text `gosub_return`, not `return`. Fix: emit gosub/goto/label with the standard prefix$ indent, emit gosub_return for the no-value RETURN, delete the stray END IF (verified a no-op for Rust: s0 byte-identical before/after), rename the $-collision. Regression lock is pre-existing and mutation-proven by the bisect: bootstrap.rs::compiler_self_compilation_produces_identical_ir failed pre-fix and passes post-fix. Only compiler.x has these constructs (lexer/parser/cgen/xut have 0 bare RETURN, 0 label, 0 GOSUB code lines), so the new arms cannot fire in the other selfhost tools. Golden fixtures/corpus/v0.1/selfhost/compiler.ir refreshed 1608 -> 2963 lines per the 745664d convention."
    source: "git bisect over selfhost/compiler.x revisions; checks/verify-bootstrap.sh lines 90-133 replicated verbatim; cargo test --release -p xb-runtime --test bootstrap,corpus,native_facet_gap -- --test-threads=1"
    attester: "session verification 2026-09-07"
    revision: "67110bb"
    exit_status: 0
    receipt: "session tool output 2026-09-07: RUST_IR==STAGE1_IR, STAGE1_IR==STAGE2_IR, lexer/parser/cgen native==rust, cgen1 C==cgen2 C (all OK, 9.8 s); bootstrap 2/2, corpus 11/11, native_facet_gap 1 passed/1 ignored; clippy --workspace --all-targets 0 warnings"
    freshness_kind: recheck_date
    date_checked: "2026-09-07"
  - claim: "BLOCKER (pre-existing at HEAD, NOT introduced this session, requires a constitutional edit): checks/verify-bootstrap.sh fails at its unsafe-allowlist gate (line 40) on crates/xb-runtime/tests/cgen_selfhost.rs:42-43, which calls libc::getrusage(RUSAGE_CHILDREN) via `unsafe` for the CGEN-OOM peak-RSS lock. Those lines are committed (added by the CGEN-OOM lock commits 9f6d816/fb649fa) and the UNSAFE_ALLOW ERE at line 36 was never extended to cover the file. checks/ is a constitutional path (oracle-manifest.yaml line 15) so the prescribed one-line allowlist edit is a maintainer out-of-band action, not an in-run change; rewriting the measurement instead would alter an OOM behavior gate and has no safe std equivalent for RUSAGE_CHILDREN. Consequence: verify-bootstrap.sh exits 1 before its post-line-42 stages. Those stages were therefore replicated verbatim this session and all pass, and the earlier stages (cargo fmt/check/test --workspace/clippy) run before the gate and pass. PROPOSED constitutional update (NOT applied): extend line 36 UNSAFE_ALLOW with '|^crates/xb-runtime/tests/cgen_selfhost\\.rs:' and add the matching allowlist comment line documenting the getrusage peak-RSS probe."
    source: "checks/verify-bootstrap.sh lines 31-42; governed-delivery/oracle-manifest.yaml constitutional_paths; git show HEAD:crates/xb-runtime/tests/cgen_selfhost.rs"
    attester: "session review 2026-09-07"
    revision: "67110bb"
    exit_status: 1
    receipt: "session tool output 2026-09-07: 'verify-bootstrap: unsafe outside the sanctioned FFI/LLVM allowlist requires a separate Miri-backed task' with cgen_selfhost.rs:42-43 listed; unsafe present in git show HEAD (pre-existing); no constitutional file modified (git status clean)"
    freshness_kind: recheck_date
    date_checked: "2026-09-07"
  - claim: "P2 dual use-walk narrowed from 22 native-only/23 rust-only (45 differing names, session baseline at 639989a) to 2 native-only/8 rust-only (10) at c69768d over the 234-program ratchet universe, with the allStrArr P1 gate green throughout and the bootstrap fixed point re-verified after every commit. Eight rules landed, each mutation-verified by an isolated Rust-vs-native probe pair that disagrees without the change: (1) LEN/SIZE open a SizeOf context (new uSzDep counter) so `LEN(a[])` names no scalar while `IFZ a[]`/`&a[]` still do — an earlier blanket uCallDepth guard was rejected because it also suppressed `&segment[]` and cost 13 new rust-only names; (2) a shared composite declared at file scope suppresses its plain facet program-wide (arecord #globaltype0..5, 6 names); (3) CFUNCTION defines a function scope and closes with END FUNCTION (xit XxxXitMain, 7 of xit's 10 names); (4) composite-typed array params (TOKEN tok[], DISPLAY d[]) emit no plain facet, mirroring composite DIMs, and a bare type qualifier is not a scalar param name (xcol 4, xgr 3); (5) a suffixed sibling of a composite emits no plain facet because Rust keys its per-scope seen set on the stripped name (display$ beside DISPLAY display[], 3 names); (6) a depth-0 colon ends the statement so declaration context stops leaking into `DIM temp[3] : SWAP values[grid,], temp[]` (4 names); (7) the $-collision set that keeps a scalar key suffixed now includes unsuffixed scalar DECLARATIONS as well as params (STATIC window$ beside STATIC window; XuiCanNumberToName(can, can$)); (8) SWAP operands keep their suffixed spelling because Rust lowers `SWAP t$, tt$[n]` to an assignment targeting t$, unlike a plain assignment which strips to t. One candidate was tried and REVERTED as a regression: passing the Phase A scope into uStripSfx activates its collision clauses on declarations and flips `DIM text[3]` + `DIM text$` from Rust-matching dual=1 to dual=0; the stale ucurScope$ is load-bearing and is now documented in uStripSfx as use-walk-only by design. Remaining 10 are long-tail single names (ToolkitCode gridFunc$/s$, XuiFile dir$, XuiQueueCallbacks callbacks, RunJump text$, XxxXitMain arg, XstLoadArrayData temp, XgrRegisterIcon sicon, Expresso farg, ParseQBasic qbasic), each needing per-function forensics rather than a shared rule."
    source: "cargo test --release -p xb-runtime --test native_facet_gap -- --test-threads=1 (P1 gate) and --ignored (P2 tracker); per-rule probe pairs via xb --emit-ir-facets vs compiled compA; xb --emit-ir vs xb --run self-image after every commit; cargo test --workspace -- --test-threads=1; cargo fmt --check; cargo clippy --workspace --all-targets"
    attester: "cargo test runner"
    revision: "c69768d"
    exit_status: 0
    receipt: "session tool output 2026-09-07: P1 gate ok (1 passed, 1 ignored) after every commit; P2 tracker TOTAL 2 native-only, 8 rust-only; workspace 373 passed / 0 failed / 1 ignored across 42 binaries; fmt clean; clippy 0 warnings; bootstrap 2/2 and corpus 11/11 per commit; native bootstrap stages all OK (RUST_IR==STAGE1_IR, STAGE1_IR==STAGE2_IR, lexer/parser/cgen native==rust, cgen1 C==cgen2 C)"
    freshness_kind: recheck_date
    date_checked: "2026-09-07"
  - claim: "SUPERSEDES the two proxy clauses in the prior entry: suffix retention now mirrors crates/xb-compiler/src/semantics_suffix.rs directly, taking P2 dual from 2 native-only/8 rust-only (10) at c69768d to 2 native-only/5 rust-only (7) at 517f56d. Ground truth read from source: slot_name() keeps a suffix only for a STRING scalar and only when collisions.contains(base); every non-string suffix (#, %, !, &&) always takes the bare base; collisions comes solely from scan_body_collisions(&inlined) (semantics_function.rs:25), which flags a base referenced with BOTH a string and a non-string type. note_var fires on scalar Dim (size.is_none() && !is_array), Assignment/Inc/Dec/Read targets, For vars, BOTH Swap operands, and Identifier/ByRefIdentifier; it never fires on an ArrayAccess or ArrayAssignment BASE (only their index/value are scanned) and skips dotted names. Implementation: a new VAR-SUFFIX-COLLISION pre-pass walks the body once before the use-walk, tracking scope exactly like the use-walk (FUNCTION/CFUNCTION, END FUNCTION, DECLARE/EXTERNAL skip) and recording into fNonStr$ every bare non-string ident that is not followed by [ or ( or . and not preceded by . ; uStripSfx then keeps a `$` iff the base is an unsuffixed ARRAY declaration in scope (fArrDim$) OR is in fNonStr$. The array-declaration half was NOT deducible from the note_var call sites and was established empirically instead: DIM v[], DIM v[3], DIM v[n], SHARED v[] and STATIC v[] all make a sibling v$ keep its suffix, while a program with no `v` array and no non-string `v` reference strips it. A first attempt that used fNonStr$ ALONE regressed the corpus 10 -> 12 (new native-only AssignAddress m_addr, XgrRegisterIconColor ico, SaveCursor cursorByte, plus rust-only m_addr$/r_addr$/op$) and was corrected to the union, which closed ToolkitCode gridFunc$/s$, XuiFile dir$ and XxxXitMain arg. One measurement error was caught and corrected mid-investigation: `grep -c 'assign m_addr\\$:string'` also matches `array_assign m_addr$:string[...]`, which briefly made a stripped case look retained. fPScalar$ (params + unsuffixed scalar declarations, added 5416a50) became write-only under the new rule and was deleted rather than left as a second convention. Remaining 7: RunJump text$ and XuiQueueCallbacks callbacks (native-only); XxxXitMain arg, XstLoadArrayData temp, XgrRegisterIcon sicon, Expresso farg, ParseQBasic qbasic (rust-only). Cost: the pre-pass adds ~10s to the P1 gate (17s -> 27s)."
    source: "crates/xb-compiler/src/semantics_suffix.rs (slot_name, scan_body_collisions, note_var, scan_stmt, scan_expr); crates/xb-compiler/src/semantics_function.rs:25; empirical xb --emit-ir probes for the array-declaration half; cargo test --release -p xb-runtime --test native_facet_gap -- --test-threads=1 [--ignored]; cargo test --workspace -- --test-threads=1"
    attester: "cargo test runner + source read"
    revision: "517f56d"
    exit_status: 0
    receipt: "session tool output 2026-09-07: 6/6 probe pairs agree (m_addr int+string arrays, dir$[dir], gridName$ no-collision, text$ beside text[], can param, SWAP); P2 tracker TOTAL 2 native-only, 5 rust-only; P1 gate ok; bootstrap 2/2; corpus 11/11; workspace 42 binaries 373 passed / 0 failed / 1 ignored; fmt clean; clippy 0 warnings; native bootstrap stages all OK"
    freshness_kind: recheck_date
    date_checked: "2026-09-07"
  - claim: "P2 (docs/19 §9.5 'Dual: use-walk + divert rules') is CODE-COMPLETE at 9da8159: the ignored P2 tracker over the 234-program ratchet universe reads 1 native-only / 4 rust-only (5 names), down from 22/23 (45) at the session baseline 639989a, with the P1 allStrArr gate green and the compiler.x self-compilation fixed point re-verified after every commit. Three further rules landed after the collision work: (a) step over the shared GROUP in `SHARED /cb/ CALLBACKS callbacks[]` - the leading `/` aborted the whole SHARED arm, so a composite never reached fCompVars$ and a later `DIM callbacks[..]` wrongly emitted a plain facet (13 group-form declarations exist corpus-wide, all SHARED); (b) a scalar that only INHERITS shared storage from a same-named shared array is still a LOCAL scalar for dual purposes - only an explicit `SHARED name` statement is non-dual. Verified matrix beside `SHARED sicon[]`: `XLONG sicon` dual=1, `AUTOX sicon` dual=1, `DIM sicon` dual=1, `STATIC sicon` dual=1 (already correct via the fRedimMode=2 carve-out), `SHARED sicon` dual=0; implemented with an fInherit flag in both the DIM and TYPENAME declarator paths. RESIDUAL 5 names are all P3/P4-owned, not P2, established by dumping both engines' facets per scope rather than inferred: RunJump text$ (native emits a dyn byref=1 facet Rust does not - fDynLocal$ classifies an undeclared @text$[] forwarded to a PLAIN array param, and the isolated repro agrees on both sides so the trigger needs full-file callee context); XxxXitMain arg, Expresso farg, XstLoadArrayData temp, ParseQBasic qbasic (Rust emits storage=dyn dual=1, three with byref=1) - these turn on dyn-local/descriptor classification, i.e. P3's storage= gate and P4's descriptor fixpoint. Expresso farg additionally needs the composite-plus-plain case (`AUTOX FUNCARG farg[]` AND `AUTOX FUNCARG farg`, where Rust emits both member facets and a plain farg) which the current composite-suppression rule folds away. Two unrelated native-only facet-existence defects were observed while classifying and are NOT dual diffs (both dual=0, so no gate flags them): native emits a bogus param facet named `ANY` in XstLoadArrayData, and spells ParseQBasic's `program` as `Program` (uCanonName casing). docs/19 §9.8 records the rules, the two documented traps, and this residual; §9.5 P2 and the §9 header were flipped from PROPOSED to LANDED."
    source: "cargo test --release -p xb-runtime --test native_facet_gap -- --test-threads=1 [--ignored]; per-rule probe pairs (xb --emit-ir-facets vs compiled compA); per-scope facet dumps for the 5 residual names; xb --emit-ir vs xb --run self-image per commit; cargo test --release -p xb-runtime --test bootstrap,corpus,cgen_cemitter_sync,docs_adopted_wording_guard -- --test-threads=1"
    attester: "cargo test runner"
    revision: "9da8159"
    exit_status: 0
    receipt: "session tool output 2026-09-07: P2 tracker TOTAL 1 native-only, 4 rust-only; P1 gate ok (1 passed, 1 ignored); 7/7 probe pairs agree on the inherit matrix incl. SHARED/STATIC/initvar/emitasm regression guards; bootstrap 2/2; corpus 11/11; cgen_cemitter_sync 115/115; docs_adopted_wording_guard 1/1; fixed point OK at each commit"
    freshness_kind: recheck_date
    date_checked: "2026-09-07"
  - claim: "P3 (docs/19 §9.5 'Dyn: DIM-order/counts + force rules') is CODE-COMPLETE: corpus-wide storage= comparison on shared facet keys over the 234-program universe reads 0 mismatches (true HEAD baseline 313 across 43 programs), facet presence byte-identical to HEAD (106 diffs before and after, exact same set), P1 allStrArr gate green, fixed point OK. Six rules landed, each preceded by a disagreeing Rust-vs-native probe pair: (a) DIM-count >= 2 incl. scalar declarators and REDIM (fDimSeen$/fDyn$); (b) late use-before-DIM incl. DIM size exprs (fFirstDim$/fLate$, declarator-START positions so self-visits compare equal); (c) unsized DIM a[] (fEnd = fsp + 2); (d) nested DIMs under IF/IFZ/IFT/IFF/FOR/WHILE/DO/SELECT with single-line-IF and ELSE-IF guards (fNest/fNestSingle); (e) GOSUB functions; (f) bare RETURN lowers to GosubReturn like GOSUB (xcol InitArrays has zero GOSUBs, one bare RETURN) while RETURN <expr> forces nothing. Load-bearing details: VAL demands a clean numeric token (VAL('1 dual=0') = 0, leading space OK); declaration-statement ident visits at urdep = 0 are not touches (Xgr SHARED FUNCADDR ehelp[] before DIM ehelp[] must not read as late) or ehelp/event falsely flip dyn. RESIDUAL storage: none. xit EditAbandon/text$ (dyn dual=0, @text$[] into TokenArrayToText descriptor=1 param) is descriptor-local force = P4 call-graph fixpoint. Presence residuals (106) are P4/P5 classes, untouched by P3. Two methodology notes: a stale /tmp/compA from a dirty tree once faked 420 mismatches plus phantom P2 holes (basic dual=1 'failing', ehelp as shared) - rebuilt from git show HEAD: and the true caseload was 313 with P2 exactly as ledgered (re-verified ignored tracker: 1 native-only + 4 rust-only, same 5 names); and a PUT N.=M range slip deleted one ELSE in the TYPENAME declarator-continuation chain (silently dropping GetExternalAddresses/pro), caught by the presence-identity check and restored - zero deletions confirmed in the final diff."
    source: "21-pair probe battery (xb --emit-ir-facets vs rebuilt compA3, incl. negatives: use-after-DIM stays fixed, single-line IF does not leak, RETURN <expr> does not force); corpus storage oracle + presence-identity oracle (HEAD vs P3 sets); cargo test --release -p xb-runtime --test native_facet_gap,bootstrap,corpus,docs_adopted_wording_guard -- --test-threads=1; cargo test --release -p xb-runtime --test cgen_cemitter_sync -- --test-threads=1"
    attester: "cargo test runner"
    revision: "HEAD"
    receipt: "session tool output 2026-09-07: battery 21/21 OK; storage 313 -> 0; presence identical (106); P1 gate ok (1 passed, 1 ignored); P2 tracker re-verified 1 native-only + 4 rust-only (same 5); bootstrap 2/2; corpus 11/11; docs_adopted_wording_guard 1/1; cgen_cemitter_sync 115/115; workspace 42 binaries 373 passed / 0 failed / 1 ignored; fmt clean; clippy 0 warnings; fixed point OK; git diff 218 insertions, 0 deletions in selfhost/compiler.x"
    freshness_kind: recheck_date
    date_checked: "2026-09-07"
